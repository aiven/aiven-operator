
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: pgs.k8s-operator.aiven.io
spec:
  group: k8s-operator.aiven.io
  names:
    kind: PG
    listKind: PGList
    plural: pgs
    singular: pg
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: PG is the Schema for the pgs API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: PGSpec defines the desired state of PG
          properties:
            cloud_name:
              description: Cloud the service runs in.
              maxLength: 256
              type: string
            maintenance_window_dow:
              description: Day of week when maintenance operations should be performed.
                One monday, tuesday, wednesday, etc.
              enum:
              - monday
              - tuesday
              - wednesday
              - thursday
              - friday
              - saturday
              - sunday
              - never
              type: string
            maintenance_window_time:
              description: Time of day when maintenance operations should be performed.
                UTC time in HH:mm:ss format.
              maxLength: 8
              type: string
            plan:
              description: Subscription plan.
              maxLength: 128
              type: string
            project:
              description: 'x-kubernetes-immutable: true Target project.'
              format: ^[a-zA-Z0-9_-]*$
              maxLength: 63
              type: string
            project_vpc_id:
              description: Identifier of the VPC the service should be in, if any.
              maxLength: 36
              type: string
            service_name:
              description: 'x-kubernetes-immutable: true Service name.'
              maxLength: 63
              type: string
          required:
          - project
          - service_name
          type: object
        status:
          description: PGStatus defines the observed state of PG
          properties:
            cloud_name:
              description: Cloud the service runs in.
              maxLength: 256
              type: string
            maintenance_window_dow:
              description: Day of week when maintenance operations should be performed.
                One monday, tuesday, wednesday, etc.
              enum:
              - monday
              - tuesday
              - wednesday
              - thursday
              - friday
              - saturday
              - sunday
              - never
              type: string
            maintenance_window_time:
              description: Time of day when maintenance operations should be performed.
                UTC time in HH:mm:ss format.
              maxLength: 8
              type: string
            plan:
              description: Subscription plan.
              maxLength: 128
              type: string
            project:
              description: Target project.
              format: ^[a-zA-Z0-9_-]*$
              maxLength: 63
              type: string
            project_vpc_id:
              description: Identifier of the VPC the service should be in, if any.
              maxLength: 36
              type: string
            service_host:
              description: PostgreSQL hostname
              type: string
            service_name:
              description: Service name.
              maxLength: 63
              type: string
            service_password:
              description: Password used for connecting to the PostgreSQL service.
              type: string
            service_port:
              description: PostgreSQL service port.
              type: integer
            service_uri:
              description: URI for connecting to the PostgreSQL service.
              type: string
            service_username:
              description: Username used for connecting to the PostgreSQL service.
              type: string
            state:
              description: PostgreSQL Service state
              type: string
            status:
              description: Service status
              type: string
          required:
          - project
          - service_name
          - status
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
