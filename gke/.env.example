# Your Google Cloud Project ID (required)
PROJECT_ID=my-gcp-project-id

# GCP Region for the cluster
REGION=europe-west1

# Developer prefix for unique naming (auto-detected from whoami, can override)
# All resource names will be prefixed with this to avoid conflicts
# Leave empty for auto-detection
DEV_PREFIX=

# GKE Cluster name
# Will become: {DEV_PREFIX}-aiven-operator-dev
CLUSTER_NAME=aiven-operator-dev

# Name for the Google Container Registry repository  
# Will become: {DEV_PREFIX}-aiven-operator
IMAGE_REPO_NAME=aiven-operator

# Your Aiven project name (required)
AIVEN_PROJECT=my-aiven-project

# Your Aiven API token (required)
AIVEN_TOKEN="your-aiven-api-token"

# Kubernetes version for the GKE cluster (1.26+ required)
KUBERNETES_VERSION=1.32

# --- Network Security ---
# OPTIONAL: Override for cluster master access.
# Leave blank for Terraform to auto-detect your IP.
# Set to "0.0.0.0/0" to allow all IPs (not recommended).
MY_PUBLIC_IP=

# --- Add-ons and Operator ---
ENABLE_OPERATOR_WEBHOOKS=true
OPERATOR_NAMESPACE=aiven-operator-namespace
OPERATOR_LOG_LEVEL=info
